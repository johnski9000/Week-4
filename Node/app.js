const {minus, multiply, add} = require("./cleverFunctions");
let cleverFunctions = require("./cleverFunctions");
let maths = require("./maths")

// console.log(cleverFunctions);
maths.multiplyy(10, 2)
cleverFunctions.add(1, 2)